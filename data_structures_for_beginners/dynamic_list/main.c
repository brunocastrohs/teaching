#include <stdio.h>
#include <stdlib.h>
#include "list.h"

int main(){

    List *list = create();
    add(list, 5);
    add(list, 10);
    add(list, 15);
    add(list, 5);
    print(list);
    search(list, 5);

function timeConversion(s) {
    
    if(s && s.match(/^(0[0-9]|1[0-2]):[0-50-9]{2}:[0-50-9]{2}PM$/)){
        
    }
    else if(s && s.match(/^(0[0-9]|1[0-2]):[0-50-9]{2}:[0-50-9]{2}AM$/)){
        
    }

}

}